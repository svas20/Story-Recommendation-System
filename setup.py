from setuptools import setup, find_packages

setup(
    name="AI_Agent_Engineer_Takehome",           # Name of the installable package
    version="0.1.0",
    packages=find_packages(),    # Automatically finds 'agent_code'
    install_requires=[]          # Add any required dependencies here
)
